# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Runtime files
.flow-local.lock

# Personal settings — config.json holds your vocabulary, corrections, and
# personal names, so it stays out of the repo. Flow Local recreates it
# with defaults on first run.
config.json

# Words auto-learned from your dictations — personal data, never shared.
learned_words.json

# Generated at build time by make_icon.py
icon.icns

# Local-only companion experiments - never publish these
flowmaxxing/
pinch_poc.py
